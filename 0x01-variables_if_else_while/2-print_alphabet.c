#include <stdio.h>
/**
 * main - enty point for the program
 * display and alpha and omega
 * Return: 0 when program complete
 */
int main(void)
{
char a = 97;
while (a < 123)
{
putchar (a);
a++;
}
putchar (10);
return (0);
}
